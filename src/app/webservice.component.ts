import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'webservice-app',
  templateUrl: 'webservice.component.html',
  styleUrls: ['webservice.component.css']
})
export class WebserviceAppComponent {
  title = 'webservice works!';
}
